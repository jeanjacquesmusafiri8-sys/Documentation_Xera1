# Docusaurus configuration and site files

This commit adds Docusaurus v3 configuration, moves MDX files to `docs/`, removes Mintlify files, and adds a GitHub Actions workflow to build and deploy to GitHub Pages.
